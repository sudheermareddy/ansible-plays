---
- hosts: oke-tlrdb-node
  connection: ssh
  gather_facts: False
  vars_files:
    - 'vars.yml'
  become: yes
  tasks:
    - name: OKE PRD TLRDB backup
      become: yes
      script: /home/QLOUDABLEINC/automationuser/ansible/playbooks/scripts/oke-prd-tlrdb-backup.sh
      register: output
      failed_when: '"Error" in output.stdout'
    - debug: var=output

