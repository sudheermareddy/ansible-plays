---
- hosts: oke-db-node
  connection: ssh
  gather_facts: False
  vars_files:
    - 'vars.yml'
  become: yes
  tasks:
    - name: PRD RDB AssessmentEnrollmentdata upload
      become: yes
      script: /home/QLOUDABLEINC/automationuser/ansible/playbooks/scripts/ExtractUploadAssessmentEnrollmentData.sh
      register: output
      failed_when: '"Error" in output.stdout'
    - debug: var=output

