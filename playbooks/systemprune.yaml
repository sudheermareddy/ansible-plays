---
- hosts: okenodes
  become: true
  vars_files:
    - 'vars.yml'
  tasks:
    - name: Docker Syetm prune
      become: true
      command: docker system prune -fa
      register: output
      #failed_when: '"Error" in output.stdout'
    - debug: var=output.stdout_lines

