---
- hosts: oke-rdb-nodes
  connection: ssh
  gather_facts: False
  vars_files:
    - 'vars.yml'
  become: yes
  tasks:
    - name: PHX PRD RDB AssessmentEnrollmentdata upload
      become: yes
      script: ./scripts/ExtractUploadAssessmentEnrollmentData.sh
      register: output
      failed_when: '"Error" in output.stdout'
    - debug: var=output


